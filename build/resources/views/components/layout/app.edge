<!DOCTYPE html>
<html lang="en" data-theme="light">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
      {{ title || 'Dashboard' }}
    </title>

    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css"
    />
    @vite('resources/css/app.css')

    @if ($slots.meta)
      {{{ await $slots.meta() }}}
    @endif
  </head>
  <body class="bg-base-100 text-gray-900">

    <div class="drawer lg:drawer-open">
      <input id="my-drawer-2" type="checkbox" class="drawer-toggle" />
      <div class="drawer-content flex flex-col">

        <!-- Sticky Navbar -->
        <div class="top-0 z-50 w-full navbar bg-base-200 px-6 shadow-sm">
          <div class="flex-none lg:hidden">
            <label for="my-drawer-2" class="btn btn-ghost btn-square text-lg">
              <i class="fa-solid fa-bars"></i>
            </label>
          </div>
          <div class="flex-1 px-2 text-2xl font-semibold">
            Data Pegawai & Pejabat
          </div>
        </div>

        <!-- Page content here -->
        <main class="p-6 bg-base-100">
          {{{ await $slots.main() }}}
        </main>
      </div>

      <!-- Sidebar -->
      <div class="drawer-side">
        <label for="my-drawer-2" class="drawer-overlay"></label>
        <aside class="w-64 min-h-screen bg-base-200 shadow-md">

          <div class="px-6 py-4 text-3xl font-bold  mb-4">
            Menu
          </div>

          <ul class="text-lg space-y-2 px-3 text-gray-800">
            <li>
              <a
                href="{{ route('dashboard') }}"
                class="flex items-center gap-3 py-2 px-3 rounded-md hover:bg-base-200 transition"
              >
                <i class="fa-solid fa-house"></i>Dashboard
              </a>
            </li>
            <li>
              <a href="#" class="flex items-center gap-3 py-2 px-3 rounded-md hover:bg-base-200 transition">
                <i class="fa-solid fa-users"></i>Pegawai
              </a>
            </li>
            <li>
              <a href="#" class="flex items-center gap-3 py-2 px-3 rounded-md hover:bg-base-200 transition">
                <i class="fa-solid fa-briefcase"></i>Jabatan
              </a>
            </li>
            <li>
              <a href="#" class="flex items-center gap-3 py-2 px-3 rounded-md hover:bg-base-200 transition">
                <i class="fa-solid fa-building"></i>Unit Kerja
              </a>
            </li>
          </ul>
        </aside>
      </div>
    </div>

    @vite('resources/js/app.js')
  </body>
</html>
